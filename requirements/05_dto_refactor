Refactor DTOs with the following rules:

Addresses
* For list responses, rename AddressResponse to AddressListResponse
* For detail responses, rename AddressDTO to AddressDetailResponse
* For create requests, create a new CreateAddressRequest DTO class, add validation rules based on the Address entity
* For update requests, create a new UpdateAddressRequest DTO class, add validation rules based on the Address entity

Apply same rules for:
* CartDTO and CartResponse
* ProductDTO and ProductResponse
* CategoryDTO and CategoryResponse

For cartItemDTO which is not returned directly, rename it to CartItem

Goals:
* Improve security without the need of providing ids for create and update requests
* Improve readability and maintainability
* Separation of concerns, Entity representation, requests and responses

Test:
* Fix unitest according to the new rules